{
  "name": "bodyclockfm",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:wrangler": "astro build && wrangler dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy && sleep 10 && curl -s -X POST https://bodyclock.fm/api/notify-new -H 'Origin: https://bodyclock.fm' | jq ."
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/mdx": "^6.0.3",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
